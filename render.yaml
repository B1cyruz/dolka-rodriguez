# Configuration for Render Static Site Blueprint
services:
  - type: web
    name: dolka-rodriguez
    env: static
    buildCommand: npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
